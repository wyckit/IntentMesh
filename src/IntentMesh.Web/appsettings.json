{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_AllowedHosts_note": "Local default is loopback only. Behind a reverse proxy, set AllowedHosts to your public host(s) (e.g. 'mesh.example.com') — do not use '*'.",
  "AllowedHosts": "localhost;127.0.0.1;[::1]"
}
